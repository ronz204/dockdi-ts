---
"dockdi": patch
---

Set up the automated release pipeline: Changesets for versioning/changelog, and a GitHub Actions workflow that publishes to npm via Trusted Publishing (OIDC), with no stored npm token.
